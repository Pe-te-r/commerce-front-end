// Example constants and helpers
export const isAuthenticated = (user: undefined) => !!user; // Helper function
export const AUTH_DEFAULT_STATE = { isLoggedIn: false }; // Example constant for default state
